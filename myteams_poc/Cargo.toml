[package]
name = "myteams_poc"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.2", features = ["poll"] }

[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor"

